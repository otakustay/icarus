/**
 * @file 窗口类
 * @author otakustay
 */

'use strict';

/**
 * 窗口类
 */
module.exports = class BrowserWindow {

    /**
     * 发送指令
     *
     * @param {string} channel 指令名称
     * @param {*} arg 参数
     */
    send(channel, arg) {
    }
};
